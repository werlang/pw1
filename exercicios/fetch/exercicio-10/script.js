import showToast from "./components/toast.js";

const photosGrid = document.querySelector('#photosGrid');
const pagination = document.querySelector('#pagination');

// TODO: Implemente aqui
// 1. Crie loadPhotos(page = 1, limit = 12) para GET /api/photo/list.php?page=X&limit=Y
// 2. Exiba fotos em grid (use <img> com as URLs)
// 3. Crie botões de paginação (Anterior, 1, 2, 3, Próximo)
// 4. Desabilite botões apropriadamente (Anterior na página 1, etc)
